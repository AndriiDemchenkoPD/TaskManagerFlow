# Default mode uses local Mailpit SMTP (works out-of-the-box in Docker).
# Mailpit inbox UI: http://localhost:8025
# To use real SMTP (e.g. Gmail), replace values below.

AUTH_RESET_PASSWORD_URL=http://localhost:5173/reset-password

SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_ENABLE_SSL=false
SMTP_FROM=noreply@taskflow.local
SMTP_USERNAME=
SA_PASSWORD=<<SA_PASSWORD_PLACEHOLDER>>
BT_CONNECTION=Server=sqlserver,1433;Database=BasicTraining;User Id=<<DB_USER>>;Password=<<DB_PASSWORD>>;TrustServerCertificate=True;
JWT_KEY=<<JWT_SECRET_PLACEHOLDER>>
SMTP_PASSWORD=
